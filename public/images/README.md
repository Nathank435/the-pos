# Site images

- `hero-pos.png` — homepage hero product shot (POS terminals + a contactless phone
  payment, on white/transparent). Shown at ~4:3. Until added, the hero shows a branded
  placeholder panel.
- `hubs/<slug>.jpg` — business-type hub banners (21:9). See `hubs/README.md`.

`<EditorialImage>` applies the house treatment and falls back to a branded placeholder,
so missing files never break a layout.
